import { FC } from 'react'
import { useTranslation } from 'react-i18next'
import i18next from 'i18next'

export const HistoricPunchIn: FC = () => {
    const { t } = useTranslation()

    return <div>Historic</div>
}
